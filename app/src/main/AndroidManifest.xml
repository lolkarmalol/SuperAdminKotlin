<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_etapaproductiva"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_etapaproductiva"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuperAdmin"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SuperAdmin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".AdministradoresActivity" />
        <activity android:name=".InstructorActivity"/>
        <activity android:name=".ApprenticeActivity"/>
        <activity android:name=".GraphicActivity"/>
        <activity android:name=".Notificaciones"/>
        <activity android:name=".Perfil_Aprendiz"/>
        <activity android:name=""/>
    </application>

</manifest>